/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { OpenBillingCore } from "../core.js";
import { encodeJSON, encodeSimple } from "../lib/encodings.js";
import * as M from "../lib/matchers.js";
import { compactMap } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { RequestOptions } from "../lib/sdks.js";
import { extractSecurity, resolveGlobalSecurity } from "../lib/security.js";
import { pathToFunc } from "../lib/url.js";
import { APIError } from "../models/errors/apierror.js";
import {
  ConnectionError,
  InvalidRequestError,
  RequestAbortedError,
  RequestTimeoutError,
  UnexpectedClientError,
} from "../models/errors/httpclienterrors.js";
import * as errors from "../models/errors/index.js";
import { SDKValidationError } from "../models/errors/sdkvalidationerror.js";
import * as operations from "../models/operations/index.js";
import { Result } from "../types/fp.js";

export async function subscriptionsPause(
  client: OpenBillingCore,
  request: operations.SubscriptionPauseSubscriptionRequest,
  options?: RequestOptions,
): Promise<
  Result<
    operations.SubscriptionPauseSubscriptionResponseBody,
    | errors.SubscriptionPauseSubscriptionResponseBody
    | errors.SubscriptionPauseSubscriptionSubscriptionsResponseBody
    | errors.SubscriptionPauseSubscriptionSubscriptionsResponseResponseBody
    | APIError
    | SDKValidationError
    | UnexpectedClientError
    | InvalidRequestError
    | RequestAbortedError
    | RequestTimeoutError
    | ConnectionError
  >
> {
  const parsed = safeParse(
    request,
    (value) =>
      operations.SubscriptionPauseSubscriptionRequest$outboundSchema.parse(
        value,
      ),
    "Input validation failed",
  );
  if (!parsed.ok) {
    return parsed;
  }
  const payload = parsed.value;
  const body = encodeJSON("body", payload.RequestBody, { explode: true });

  const pathParams = {
    subscription_id: encodeSimple("subscription_id", payload.subscription_id, {
      explode: false,
      charEncoding: "percent",
    }),
  };

  const path = pathToFunc("/subscription/{subscription_id}/pause")(pathParams);

  const headers = new Headers(compactMap({
    "Content-Type": "application/json",
    Accept: "application/json",
  }));

  const securityInput = await extractSecurity(client._options.security);
  const requestSecurity = resolveGlobalSecurity(securityInput);

  const context = {
    operationID: "subscription:pauseSubscription",
    oAuth2Scopes: [],

    resolvedSecurity: requestSecurity,

    securitySource: client._options.security,
    retryConfig: options?.retries
      || client._options.retryConfig
      || { strategy: "none" },
    retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
  };

  const requestRes = client._createRequest(context, {
    security: requestSecurity,
    method: "POST",
    baseURL: options?.serverURL,
    path: path,
    headers: headers,
    body: body,
    timeoutMs: options?.timeoutMs || client._options.timeoutMs || -1,
  }, options);
  if (!requestRes.ok) {
    return requestRes;
  }
  const req = requestRes.value;

  const doResult = await client._do(req, {
    context,
    errorCodes: ["400", "404", "409", "4XX", "5XX"],
    retryConfig: context.retryConfig,
    retryCodes: context.retryCodes,
  });
  if (!doResult.ok) {
    return doResult;
  }
  const response = doResult.value;

  const responseFields = {
    HttpMeta: { Response: response, Request: req },
  };

  const [result] = await M.match<
    operations.SubscriptionPauseSubscriptionResponseBody,
    | errors.SubscriptionPauseSubscriptionResponseBody
    | errors.SubscriptionPauseSubscriptionSubscriptionsResponseBody
    | errors.SubscriptionPauseSubscriptionSubscriptionsResponseResponseBody
    | APIError
    | SDKValidationError
    | UnexpectedClientError
    | InvalidRequestError
    | RequestAbortedError
    | RequestTimeoutError
    | ConnectionError
  >(
    M.json(
      200,
      operations.SubscriptionPauseSubscriptionResponseBody$inboundSchema,
    ),
    M.jsonErr(
      400,
      errors.SubscriptionPauseSubscriptionResponseBody$inboundSchema,
    ),
    M.jsonErr(
      404,
      errors
        .SubscriptionPauseSubscriptionSubscriptionsResponseBody$inboundSchema,
    ),
    M.jsonErr(
      409,
      errors
        .SubscriptionPauseSubscriptionSubscriptionsResponseResponseBody$inboundSchema,
    ),
    M.fail("4XX"),
    M.fail("5XX"),
  )(response, { extraFields: responseFields });
  if (!result.ok) {
    return result;
  }

  return result;
}
