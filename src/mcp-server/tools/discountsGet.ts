/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { discountsGet } from "../../funcs/discountsGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DiscountGetDiscountRequest$inboundSchema,
};

export const tool$discountsGet: ToolDefinition<typeof args> = {
  name: "discounts_get",
  description: ``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await discountsGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
