/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { productsCreateWithPrices } from "../../funcs/productsCreateWithPrices.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ProductsCreateWithPricesRequestBody$inboundSchema,
};

export const tool$productsCreateWithPrices: ToolDefinition<typeof args> = {
  name: "products-create-with-prices",
  description: ``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await productsCreateWithPrices(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
