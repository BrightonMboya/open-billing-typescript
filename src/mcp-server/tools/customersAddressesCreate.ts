/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { customersAddressesCreate } from "../../funcs/customersAddressesCreate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.AddressesCreateRequest$inboundSchema,
};

export const tool$customersAddressesCreate: ToolDefinition<typeof args> = {
  name: "customers-addresses-create",
  description: ``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await customersAddressesCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
